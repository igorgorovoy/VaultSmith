
#  VaultSmith - безпечний міст між Firefox і KeePass

_Як захистити свої паролі після експорту з браузера та перетворити їх на надійно зашифровану базу._

---

###  Проблема

Браузери зберігають паролі зручно, але не завжди безпечно.  
Firefox дозволяє **експортувати логіни у CSV**, та є одна деталь: цей файл - **відкритий текст**, де всі паролі видно без шифрування.  

Для тих, хто піклується про безпеку своїх даних, цього недостатньо.

---

### ⚙️ Рішення: VaultSmith

**VaultSmith** - це легкий CLI-інструмент, створений для **безпечного шифрування паролів** після експорту з Firefox та **імпорту у KeePass**.  
Він не ламає браузерну безпеку, а навпаки - доповнює її.

---

###  Основні можливості

-  **Шифрування файлів (AES-GCM)** - надійний стандарт, що захищає від несанкціонованого доступу.  
-  **KDF PBKDF2 / Argon2** - захист від перебору паролів.  
-  **Імпорт у KeePass** - створення `.kdbx` бази з CSV-експорту Firefox.  
-  **Групування за доменами** - автоматичне впорядкування записів.  
-  **Keyfile (опціонально)** - другий фактор захисту бази.  

---

###  Архітектура VaultSmith

VaultSmith складається з трьох невеликих Python-утиліт:

| Модуль | Призначення |
|--------|--------------|
| `encrypt.py` | Шифрує CSV або будь-який файл у бінарний формат `.enc` |
| `view.py` | Розшифровує та показує вміст (тільки після введення пароля) |
| `csv_to_kdbx.py` | Конвертує CSV у `.kdbx` базу KeePass |

---

###  Як це працює

1. **Експортуєш логіни з Firefox**
   - Відкрий `about:logins` → ⋯ → **Export Logins…**
   - Збережи файл `export.csv`

2. **Шифруєш CSV**
   ```bash
   python encrypt.py export.csv export.enc
   ```
   Файл `export.enc` - тепер твій захищений контейнер.

3. **Розшифровуєш (за потреби)**
   ```bash
   python view.py export.enc
   ```

4. **Створюєш KeePass базу**
   ```bash
   python csv_to_kdbx.py export.csv my_passwords.kdbx --group-by-domain
   ```

   VaultSmith автоматично:
   - створить `.kdbx` файл,
   - запитає пароль,
   - розкладе записи по доменах.

---

###  Встановлення

Створи файл `requirements.txt`:

```text
pykeepass>=4.0.0
python-dateutil>=2.8.2
cryptography>=40.0.0
argon2-cffi>=23.1.0  # опціонально
```

Виконай інсталяцію:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

---

### Як влаштовано шифрування

VaultSmith не вигадує велосипед - він використовує перевірені криптографічні примітиви:

```python
from cryptography.hazmat.primitives.kdf.pbkdf2 import PBKDF2HMAC
from cryptography.hazmat.primitives.ciphers.aead import AESGCM
import os

salt = os.urandom(16)
kdf = PBKDF2HMAC(algorithm=hashes.SHA256(), length=32, salt=salt, iterations=200_000)
key = kdf.derive(password_bytes)
aesgcm = AESGCM(key)
nonce = os.urandom(12)
ciphertext = aesgcm.encrypt(nonce, plaintext_bytes, None)
```

>  Формат файлу: `[MAGIC][salt(16)][nonce(12)][ciphertext...]`

---

##  Перевірені криптоалгоритми: чому їм можна довіряти

Нижче - стисле обґрунтування стійкості алгоритмів, які застосовує VaultSmith, і ким фактично перевірена їхня надійність.

### AES‑GCM (AES‑256 у режимі GCM)

- **Статус:** стандарт NIST (FIPS 197 для AES, SP 800‑38D для GCM).  
- **Перевірка часом:** десятиліття відкритого криптоаналізу, масове використання в TLS, SSH, IPsec, системах з апаратним прискоренням (AES‑NI).  
- **Властивості:** AEAD - забезпечує **конфіденційність і цілісність**.  
- **Практика безпеки:** унікальний 96‑бітний nonce на кожне шифрування; ротація ключів при великих об’ємах даних.

### PBKDF2 (HMAC‑SHA256)

- **Статус:** RFC 2898, рекомендований у NIST SP 800‑132.  
- **Надійність:** перевірений і сумісний майже з усім; безпечний за достатньої кількості ітерацій (≥100k–200k).  
- **Обмеження:** CPU‑bound → порівняно вразливий до GPU/ASIC‑перебору. Для сучасних загроз краще **Argon2id**.

### Argon2 (рекомендовано Argon2id)

- **Статус:** переможець Password Hashing Competition (2015); оцінений академічною спільнотою криптографів.  
- **Сильні сторони:** **memory‑hard** дизайн, що суттєво ускладнює атаки на GPU/ASIC; настроювані параметри пам’яті/часу/паралельності.  
- **Рекомендації:** Argon2id із пам’яттю 64–256 MiB, time‑cost 2–4, parallelism 1–4; сіль 16 байт.

> ✅ **Висновок:** AES‑GCM + Argon2id - сучасна й стійка комбінація для шифрування даних, отриманих із людських паролів. PBKDF2 залишається прийнятним компромісом там, де потрібна максимальна сумісність.

---

### ⚠️ Ризики та як їх уникати

- **Повторний nonce у GCM** - критично небезпечно. Генеруй випадкові 12‑байтові nonces.  
- **Слабкі паролі** - навіть найкращий KDF не врятує коротку фразу. Використовуй **пасфрази 16–20+ символів**.  
- **Параметри KDF** - тестуй на своїй машині; підвищуй пам’ять/ітерації, поки не отримаєш прийнятний час (0.3–1.0 с на derivation).  
- **Оновлення бібліотек** - тримай `cryptography`, `argon2-cffi`, OpenSSL/libsodium у актуальному стані.

---

###  Чому KeePass

KeePass - це **децентралізований менеджер паролів**.  
Він не потребує хмари, підтримує відкриті формати та дозволяє повний контроль над файлами.  
VaultSmith автоматизує міграцію: з CSV → `.kdbx`.

---

### ⚠️ Рекомендації безпеки

- Одразу **видали CSV** після створення `.enc` або `.kdbx`.  
- Використовуй **пасфрази 16+ символів**.  
- Зберігай `.kdbx` у **зашифрованому сховищі** (наприклад, S3 з KMS).  
- Для додаткового захисту - **використовуй keyfile**.  
- Якщо працюєш у команді - не дійся master‑паролем.

---

###  Плани розвитку

- ✅ Підтримка **Argon2** як основного KDF  
- ✅ Інтеграція з KeePassXC CLI  
- 🔄 Автоматичне secure‑delete після імпорту  
- 🧭 GUI‑версія (Tauri / Electron)  
- ☁️ Опція резервного копіювання в S3 із шифруванням KMS  

---

### 📜Ліцензія та етика

VaultSmith призначено **для власних облікових даних**.  
Використання для отримання чужих паролів - **незаконне**.  
Автор не несе відповідальності за зловживання програмою.

---

### 🛠️ Підсумок

VaultSmith - це простий, але потужний спосіб **перетворити відкритий CSV з паролями на захищену KeePass базу**.  
Він поєднує перевірені криптоалгоритми (AES‑GCM + Argon2/PBKDF2), чистий код і повну автономність - без зовнішніх сервісів чи підозрілих хмар.

> 🏗️ **Ковай свої сейфи сам.**  
> VaultSmith - твоя кузня цифрової безпеки.
